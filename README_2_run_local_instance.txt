To run these git-pages locally, use the following commands from the terminal in the directory of the Gemfile

Note: There is a dependency on Ruby for this to work

>> bundle install
>> bundle exec jekyll serve --force_polling

Open web browser to http://127.0.0.1:4000/